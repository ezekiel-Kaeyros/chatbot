export enum TimeToDisableBot {
    IN_SECONDE = 60,
    IN_MINUTE = 1
}

export enum Status_bot {
    DISABLED = 'disabled',
    ENABLED = 'enabled'
}

export enum TypeWhatsappMessage {
    BUTTON_REPLY = 'button_reply',
    TEXT = 'text',
    LIST_REPLY = 'list_reply',
    INTERACTIVE = 'interactive'
}